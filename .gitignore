# Runtime data (local settings, sync state, temp downloads — never shared)
data/

# Database backups
backups/

# OS noise
Thumbs.db
desktop.ini

# Documentation interne, gardee locale, jamais publiee
*.md
!README.md
