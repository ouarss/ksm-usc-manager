# Runtime data (local settings, sync state, temp downloads — never shared)
data/

# Database backups
backups/

# OS noise
Thumbs.db
desktop.ini

# Documentation interne (structure/instructions), gardee locale, jamais publiee


